import { StytchHeadlessClient } from './index.headless.js';
import { StytchProjectConfigurationInput, StytchLoginConfig, StyleConfig, Callbacks, StringsOptions } from '@stytch/core/public';
export * from '@stytch/core/public';
import { I as IDPConsentManifestGenerator, A as AuthTokenParams } from './types-C1nZnwOa.js';
import '@stytch/core';
import './createAuthUrlHandler-kCp0W81C.js';
import 'google-one-tap';

/**
 * A client used for invoking the Stytch API an.
 * The Stytch UI Client can be used as a drop-in solution for authentication and session management.
 * The Stytch UI client also can be used to render various UI elements for managing users.
 * Full documentation can be found {@link https://stytch.com/docs/sdks/javascript-sdk online.}
 * @example
 * const stytch = new StytchUIClient('public-token-<find yours in the stytch dashboard>');
 * stytch.magicLinks.email.loginOrCreate('sandbox@stytch.com', {
 *   login_magic_link_url: 'https://example.com/authenticate',
 *   login_expiration_minutes: 60,
 *   signup_magic_link_url: 'https://example.com/authenticate',
 *   signup_expiration_minutes: 60,
 * });
 */
declare class StytchUIClient<TProjectConfiguration extends StytchProjectConfigurationInput = Stytch.DefaultProjectConfiguration> extends StytchHeadlessClient<TProjectConfiguration> {
    constructor(...args: ConstructorParameters<typeof StytchHeadlessClient<TProjectConfiguration>>);
    /**
     * Mounts a login screen inside the element provided.
     * If a login screen has already been rendered inside the element,
     * it will be updated to use the new config, style, and callback options passed in.
     * @example
     * stytchClient.mountLogin({
     *   elementId: '#login-container',
     *   config: {...}
     * });
     *
     * @param elementId - A string containing the id of the element that should contain the login screen.
     * @param config - A {@link StytchLoginConfig} object containing the products that should be registered
     * @param styles - A {@link StyleConfig} object containing custom styling info
     * @param callbacks - Optional {@link Callbacks} triggered by various events in the Stytch SDK.
     * @param strings - Optional custom strings to be used in the prebuilt UI.
     * @throws An error when the element specified by elementId cannot be found.
     */
    mountLogin({ elementId, config, styles, callbacks, strings, }: {
        elementId: string;
        config: StytchLoginConfig;
        styles?: StyleConfig;
        callbacks?: Callbacks<TProjectConfiguration>;
    } & StringsOptions): void;
    /**
     * Mounts a reset password screen inside the element provided.
     * If a reset password screen has already been rendered inside the element,
     * it will be updated to use the new config, style, and callback options passed in.
     * @example
     * stytchClient.mountResetPassword({
     *   elementId: '#login-container',
     *   config: {...}
     * });
     *
     * @param elementId - A string containing the id of the element that should contain the login screen.
     * @param config - A {@link StytchLoginConfig} object containing the products that should be registered
     * @param styles - A {@link StyleConfig} object containing custom styling info
     * @param callbacks - Optional {@link Callbacks} triggered by various events in the Stytch SDK.
     * @param strings - Optional custom strings to be used in the prebuilt UI.
     * @throws An error when the element specified by elementId cannot be found.
     */
    mountResetPassword({ elementId, config, styles, callbacks, strings, passwordResetToken, }: {
        elementId: string;
        config: StytchLoginConfig;
        styles?: StyleConfig;
        callbacks?: Callbacks<TProjectConfiguration>;
        passwordResetToken: string;
    } & StringsOptions): void;
    /**
     * Mounts a Passkey registration screen inside the element provided.
     * If a passkey registration screen has already been rendered inside the element,
     * it will be updated to use the new config, style, and callback options passed in.
     * @example
     * stytchClient.mountPasskeyRegistration({
     *   elementId: '#login-container',
     *   config: {...}
     * });
     *
     * @param elementId - A string containing the id of the element that should contain the login screen.
     * @param config - A {@link StytchLoginConfig} object containing the products that should be registered
     * @param styles - A {@link StyleConfig} object containing custom styling info
     * @param callbacks - Optional {@link Callbacks} triggered by various events in the Stytch SDK.
     * @param strings - Optional custom strings to be used in the prebuilt UI.
     * @throws An error when the element specified by elementId cannot be found.
     */
    mountPasskeyRegistration({ elementId, config, styles, callbacks, strings, }: {
        elementId: string;
        config: StytchLoginConfig;
        styles?: StyleConfig;
        callbacks?: Callbacks<TProjectConfiguration>;
    } & StringsOptions): void;
    /**
     * Mounts an IDP Consent screen inside the element provided.
     * Parses OAuth Authorization params (client_id, scope, nonce, etc.) out of the page URL.
     * Requires the user to be logged in.
     * @example
     * stytchClient.mountIdentityProvider({
     *   elementId: '#idp-container',
     *   config: {...}
     * });
     *
     * @param elementId - A string containing the id of the element that should contain the login screen.
     * @param styles - A {@link StyleConfig} object containing custom styling info
     * @param callbacks - Optional {@link Callbacks} triggered by various events in the Stytch SDK.
     * @param strings - Optional custom strings to be used in the prebuilt UI.
     * @param getIDPConsentManifest - Optional {@link IDPConsentManifestGenerator} to render custom scopes
     * @param authTokenParams - Optional {@link AuthTokenParams} to provide a trusted auth token to provide for attestation prior to running OAuth flow.
     * @throws An error when the element specified by elementId cannot be found.
     */
    mountIdentityProvider({ elementId, styles, callbacks, strings, getIDPConsentManifest, authTokenParams, }: {
        elementId: string;
        styles?: StyleConfig;
        callbacks?: Callbacks<TProjectConfiguration>;
        getIDPConsentManifest?: IDPConsentManifestGenerator;
        authTokenParams?: AuthTokenParams;
    } & StringsOptions): void;
}

type OAuthAuthorizeParams = {
    client_id: string;
    redirect_uri: string;
    response_type: string;
    scopes: string[];
    code_challenge?: string;
    state?: string;
    nonce?: string;
    prompt?: string;
    resources?: string[];
};
type OAuthLogoutParams = {
    client_id: string;
    post_logout_redirect_uri: string;
    id_token_hint?: string;
    state?: string;
};
/**
 * Parse the OAuth Authorize params from the URL search parameters to pass in to subsequent OAuthAuthorize calls.
 *
 * @param params - The URL search parameters to parse.
 * @returns The parsed OAuth Authorize parameters.
 */
declare const parseOAuthAuthorizeParams: (params: URLSearchParams) => {
    error: string | null;
    result: OAuthAuthorizeParams;
};
declare const parseOAuthLogoutParams: (params: URLSearchParams) => {
    error: string | null;
    result: OAuthLogoutParams;
};

type MessageKey = "button.back.ariaLabel" | "button.cancel" | "button.continue" | "button.createPasskey" | "button.createPasswordInstead" | "button.delete" | "button.done" | "button.edit" | "button.emailLogin" | "button.emailMagicLink" | "button.emailOTP" | "button.forgotPassword" | "button.hidePassword" | "button.loginWithPasskey" | "button.loginWithoutPassword" | "button.retry" | "button.save" | "button.showPassword" | "button.skip" | "button.sms" | "button.whatsapp" | "createAccount.title" | "crypto.connectWallet.instructionPopup" | "crypto.connectWallet.title" | "crypto.continueWithOtherWallet.title" | "crypto.error.content" | "crypto.error.title" | "crypto.setupNewWallet.content" | "crypto.setupNewWallet.instruction" | "crypto.setupNewWallet.title" | "crypto.signMessage.instruction" | "crypto.signMessage.title" | "crypto.success.content" | "crypto.success.title" | "crypto.troubleHelp" | "crypto.wallet.binance" | "crypto.wallet.coinbase" | "crypto.wallet.continueWithBinance" | "crypto.wallet.continueWithCoinbase" | "crypto.wallet.continueWithEthereum" | "crypto.wallet.continueWithMetamask" | "crypto.wallet.continueWithOtherWallet" | "crypto.wallet.continueWithPhantom" | "crypto.wallet.continueWithSolana" | "crypto.wallet.continueWithVessel" | "crypto.wallet.metamask" | "crypto.wallet.otherEthereum" | "crypto.wallet.otherSolana" | "crypto.wallet.phantom" | "crypto.wallet.setupNewWallet" | "crypto.wallet.vessel" | "emailConfirmation.content" | "emailConfirmation.title" | "error.breachedPassword" | "error.duplicatePasskey" | "error.duplicatePhoneNumber" | "error.emailNotFound" | "error.inactiveEmail" | "error.invalidCode" | "error.invalidEmail" | "error.invalidPhoneNumber" | "error.invalidPhoneNumberCountryCode" | "error.invalidSessionToken" | "error.memberPasswordNotFound" | "error.noUserPassword" | "error.oauthFlowCallbackError" | "error.oauthTokenNotFound" | "error.otpCodeNotFound" | "error.passkeyNotCreated" | "error.passkeyRegistrationNotFound" | "error.passkeyUnknown" | "error.resetPassword" | "error.sessionNotFound" | "error.title" | "error.unableToAuthMagicLink" | "error.unauthorizedAction" | "error.unauthorizedCredentials" | "error.unsubscribedPhoneNumber" | "error.userLockLimitReached" | "error.userUnauthenticated" | "errors.invalidEmail" | "formField.countryCode.label" | "formField.email.label" | "formField.email.placeholder" | "formField.otp.ariaLabel" | "formField.passcode.status.verifying" | "formField.password.label" | "formField.phone.label" | "idpConsent.allowClientTo" | "idpConsent.button.allow" | "idpConsent.button.deny" | "idpConsent.cannotGrantRequest" | "idpConsent.customScopes.title" | "idpConsent.defaultClientName" | "idpConsent.denied" | "idpConsent.noActiveSession" | "idpConsent.noPermissionsToGrant" | "idpConsent.signIn.title" | "idpLogout.confirmMessage" | "idpLogout.no" | "idpLogout.notLoggedOut" | "idpLogout.title" | "idpLogout.yes" | "link.openInGmail" | "link.openInOutlook" | "link.openInYahoo" | "login.loading" | "login.success.content" | "login.title" | "methodDivider.text" | "oauth.continueWithAmazon" | "oauth.continueWithApple" | "oauth.continueWithBitbucket" | "oauth.continueWithCoinbase" | "oauth.continueWithDiscord" | "oauth.continueWithFacebook" | "oauth.continueWithFigma" | "oauth.continueWithGitHub" | "oauth.continueWithGitLab" | "oauth.continueWithGoogle" | "oauth.continueWithLinkedIn" | "oauth.continueWithMicrosoft" | "oauth.continueWithSalesforce" | "oauth.continueWithSlack" | "oauth.continueWithSnapchat" | "oauth.continueWithTikTok" | "oauth.continueWithTwitch" | "oauth.continueWithTwitter" | "oauth.continueWithYahoo" | "otp.success.content" | "passcode.codeSent" | "passcode.didntGetIt" | "passcode.expirationCountdown" | "passcode.expired" | "passcode.resendCode" | "passcode.sentConfirmation" | "passcode.title" | "passkey.editing.label" | "passkey.registrationStart.description" | "passkey.registrationStart.error.noUser" | "passkey.registrationStart.subtitle" | "passkey.registrationStart.title" | "passkey.registrationSuccess.deleteWarning" | "passkey.registrationSuccess.existingPasskeys" | "passkey.registrationSuccess.setupAnother" | "passkey.registrationSuccess.title" | "passkey.success.content" | "password.breached.content" | "password.breached.title" | "password.createAccount.content" | "password.didntGetItResend" | "password.forgot.content" | "password.forgot.title" | "password.login.title" | "password.reset.loggingInProgress" | "password.reset.loginWithoutPassword" | "password.reset.title" | "password.resetRequired.content" | "password.resetRequired.title" | "password.setNew.content" | "password.setNew.title" | "password.strengthError.breached" | "password.strengthError.complexity" | "password.strengthError.minimumLength" | "password.zxcvbn.suggestion.addWords" | "password.zxcvbn.suggestion.allUppercase" | "password.zxcvbn.suggestion.avoidDatesAndYearsAssociatedWithYou" | "password.zxcvbn.suggestion.avoidRecentYears" | "password.zxcvbn.suggestion.avoidRepeats" | "password.zxcvbn.suggestion.avoidSequences" | "password.zxcvbn.suggestion.avoidYearsAssociatedWithYou" | "password.zxcvbn.suggestion.capitalization" | "password.zxcvbn.suggestion.longerKeyboardPattern" | "password.zxcvbn.suggestion.noSymbolsDigitsUppercase" | "password.zxcvbn.suggestion.predictableSubstitutions" | "password.zxcvbn.suggestion.reversed" | "password.zxcvbn.suggestion.useAFewWords" | "password.zxcvbn.warning.commonNames" | "password.zxcvbn.warning.dates" | "password.zxcvbn.warning.longerRepeats" | "password.zxcvbn.warning.names" | "password.zxcvbn.warning.recentYears" | "password.zxcvbn.warning.repeats" | "password.zxcvbn.warning.sequences" | "password.zxcvbn.warning.shortKeyboardPatterns" | "password.zxcvbn.warning.similarToCommonPassword" | "password.zxcvbn.warning.straightRows" | "password.zxcvbn.warning.top100CommonPassword" | "password.zxcvbn.warning.top10CommonPassword" | "password.zxcvbn.warning.veryCommonPassword" | "password.zxcvbn.warning.wordByItself" | "provider.lastUsed" | "success.title" | "tab.email" | "tab.text" | "tab.whatsapp" | "toast.emailResent" | "watermark.altText";
type Messages = Partial<Record<MessageKey, string>>;

export { StytchUIClient, parseOAuthAuthorizeParams, parseOAuthLogoutParams };
export type { OAuthAuthorizeParams, OAuthLogoutParams, Messages as Strings };
